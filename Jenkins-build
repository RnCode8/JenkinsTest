pipeline {
    agent any

    stages {
        stage('Compile') {
            steps {
                echo 'Compiling project...'
                sh 'mvn compile'
            }
        }

        stage('Trigger Test Job') {
            steps {
                echo 'Triggering test job...'
                build job: 'pipeline-test'
            }
        }
    }
}